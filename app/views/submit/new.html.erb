<%= form_tag("/submit", method: "post") do %>
  <%= text_area_tag(:message, '', placeholder: "Enter message", size: "100x6") %>
  <!-- there must be a way to make rails do that automatically -->
  <br>
  <%= submit_tag("Send") %>
<% end %>
